---
title: "Create an endpoint"
---

<Steps>
  <Step title="Add an endpoint">

    To create an endpoint go to the second tab named **Endpoints** and select **+Add Endpoint**.

    <Note>
    You can have several endpoins within the same destination - you can also decide what kind of data you want to expose in each and every endpoint.
    </Note>

  </Step>
  <Step title="Enter the endpoint description">

    Enter follwing description

    - **Name** - Name of the endpoint.
    - **Identifier** - Will be prefilled but has the possibilty to change.
    - **Description** - Description of your endpoint.

    ![An image from the static](/images/destination-endpoint-1-e2ae3f83735cbb954a58948db95c77c5.png)

  </Step>
  <Step title="Choose a segment or a card">

    Next step is to decide if the endpoint will based on a Segment or on a card.
    The difference is:

    - A segment is a selection within you card that has been created. For example I only might want products with the 'Status' = 'Active' on my endpoint - I can use the segmentation to setup filtering for this property - meaning that the product will only get exposed if its value is set to 'Active'.
    - If you select a card you will get a complete result of all the properties included on that specific card.

    ![An image from the static](/images/destination-endpoint-2-cc99c78e1135185cad4e4f70660472ce.png)

    <Tip>
    Use the **Filter** och **Search** to narrow down your result of segments and cards.
    </Tip>

    <Tip>
    To select a card or a segment click on the **Checkbox** in the lower right corner of the segment or card.
    </Tip>

  </Step>
</Steps>

**Create a endpoint using segment**

**Create a endpoint using card**
